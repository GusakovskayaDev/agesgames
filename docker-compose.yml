services:
  web:
    build: .
    container_name: ${CONTAINER_NAME:-nextjs-app}
    restart: always
    ports:
      - "${HOST_PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    networks:
      - app-network

networks:
  app-network:
    driver: bridge