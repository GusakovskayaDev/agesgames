stages:
  - deploy

deploy_staging:
  stage: deploy
  tags:
    - shell
  script:
    - echo "🛠 Начинаем деплой на Staging..."
    - cd /root/staging
    - git fetch origin
    - git checkout develop
    - git reset --hard origin/develop
    - docker-compose pull
    - docker-compose up -d --build
    - echo "Staging updated: https://staging.игрывеков.рф"
  only:
    - develop

deploy_production:
  stage: deploy
  tags:
    - shell
  script:
    - echo "🚀 Начинаем деплой на Production..."
    - cd /root/app-prod
    - git fetch origin
    - git checkout main
    - git reset --hard origin/main
    - docker-compose pull
    - docker-compose up -d --build
    - echo "Production updated: https://игрывеков.рф"
  only:
    - main